import { PieChartBaseDirective } from './pie-chart-base.directive';

describe('PieChartBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new PieChartBaseDirective();
    expect(directive).toBeTruthy();
  });
});
